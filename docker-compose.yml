services:
  mongodb:
    image: mongo:latest
    command: ["--replSet", "rs0"]
    container_name: mongodb-t360
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/import
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin
